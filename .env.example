# Copy to .env and fill in real values. .env is gitignored — never commit it.
# MySQL 8.0 local instance (service MySQL80).
DB_HOST=localhost
DB_PORT=3306
DB_NAME=olist
DB_USER=olist_app
DB_PASSWORD=<your-app-user-password>